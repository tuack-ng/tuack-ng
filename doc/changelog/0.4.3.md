1.0 - Kaslana Release Preview 4

> [!important]
>
> 本次更新对配置文件进行了破坏性更改，配置文件版本已升级至 **V7**。**旧版配置文件无法在不迁移的情况下使用。**
>
> **请在继续使用前执行迁移**：运行 `tuack-ng conf migrate` 即可自动迁移当前工程的所有配置文件。**强烈建议用户在迁移前进行备份**（如使用版本管理系统）
>
> 其中从 V4 -> V6 的迁移需要用户在迁移后手动干预，相关信息将在迁移时自动打印。

## 🚀 新增功能与优化

- 【渲染】Minijinja cases 函数可读入单个数字; 增加 data_cases sample_cases 上下文
- 【题面】支持在题面中嵌入 HTML 风格注释（`<!-- -->`）
- 【渲染】支持不显示比赛起止时间
- 【命令行】增加输出诊断信息命令
- 【模板】模板存储增加后缀名，以方便统计
- 【命令行】配置文件警告树形化，可以通过 `doc validate` 查看
- 【配置】配置迁移系统可拓展化；允许链式迁移
- 【配置】增加配置文件层级校验
- 【配置】引入 generator, checker 显式依赖; 引入 generator, checker trait
- 【配置】移除从未使用的 partial_score 字段，配置文件升级 V7

## 🐛 Bug 修复

- 【模板】升级模板中的 mitex，修复基于 typst 的模板（NOI，CCPC）无法编译的问题
- 【测试】修复 `test` 命令在编译时没有从临时文件编译，可能导致交互题评测失败的问题

<!-- generated by git-cliff -->
